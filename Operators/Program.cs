using System;

namespace Operators
{
    class Program
    {
        static void Main(string[] args)
        {
            int a = 17;
            int b = 4;
            int quotient = a / b;
            int remainder = a % b;
            if (a == 17 && b == 4);
            Console.Write($"{a}/{quotient} is {b} remainder {remainder}");
        }
    }
}
